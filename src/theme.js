import { createTheme } from '@mui/material';

const theme = createTheme({
	palette: {
		primary: {
			main: '#212121',
		},
		secondary: {
			main: '#fff',
		},
		background: {
			default: '#36353b',
		},
		text: {
			primary: '#000000',
		},
	},
});

export default theme;
