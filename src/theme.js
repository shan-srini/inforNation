import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#212121',
        },
        secondary: {
            main: '#03a9f4',
        },
        background: {
            default: '#36353b',
        },
        text: {
            primary: '#FFFFFF'
        }
    },
});

export default theme;